---
import { loadReport } from "../../data/report";

const report = loadReport();
const generatedAt = new Date(report.generated_at);
---

<footer class="bg-bg-surface border-t border-border mt-auto">
  <div class="container mx-auto px-4 py-4 max-w-7xl">
    <div class="flex flex-col md:flex-row items-center justify-between gap-2 text-sm text-text-secondary">
      <div class="flex items-center gap-2">
        <span>Fintel</span>
        <span>·</span>
        <span>Financial Intelligence Dashboard</span>
      </div>

      <div class="flex items-center gap-4">
        <span>
          Last updated: {generatedAt.toLocaleDateString()} {generatedAt.toLocaleTimeString()}
        </span>
      </div>
    </div>

    <p class="text-xs text-text-secondary text-center mt-2">
      Disclaimer: This is not financial advice. All data is for informational purposes only.
    </p>
  </div>
</footer>
